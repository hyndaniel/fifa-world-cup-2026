# 赛程图 · 决策面板重做 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把「决策面板」重做成可扫赛程(折叠行 + 筛选条 + 常驻右侧详情面板),并让决策卡按开球时刻自动衰减,根治"永不过期"的日期 bug。

**Architecture:** 后端在 `backend/state.py` 新增三个**纯函数**(`parse_ko_dt` / `ko_status` / `decisions_view`)做"开球时刻 −6h 滑窗"筛选与状态标注;`db.get_decisions()` 保持不动(继续返回全部),`/api/decisions` 在返回前过一遍 `decisions_view`;`matches_today` 改用同一滑窗。前端重写 `renderDecisions`:筛选条 + 分组折叠行 + 响应式(桌面右侧常驻面板 / 窄屏行内展开),详情**复用现有 `buildDecisionCard()`**。

**Tech Stack:** Python 3 + FastAPI(后端)、pytest(后端测试)、原生 JS/CSS 无框架无构建(前端)、Chrome headless + stdlib mock server(前端手测,沿用项目既有配方)。

## Global Constraints

- 不新增第三方依赖;后端仅用 stdlib + 现有 deps;前端纯原生 JS/CSS,无框架无构建步骤。
- 不新增接口、不新增数据表;`db.get_decisions()` 签名与行为**保持不变**(现有 `tests/test_db.py::test_get_decisions_sorted_by_ko_bj_missing_last` 必须仍通过)。
- 北京时区:`BJ = timezone(timedelta(hours=8))`(已在 `backend/state.py` 定义,复用,勿重定义)。
- 衰减窗 `DECAY_H = 6`(小时),作为函数默认参数,勿硬编码进多处。
- 响应式断点:`820px`。默认筛选:**未结束(upcoming)**。
- 详情面板内容必须复用 `frontend/app.js` 现有 `buildDecisionCard(d)`,不另写决策卡。
- 配色复用 newsprint 主题 CSS 变量(`--ink/--ink-2/--ink-3/--frame/--edge/--fair/--bleed/--bone-*`),勿引入新色值。
- 决策 dict 已可能含 `status` 字段(售卖态 Selling/Stopped),**不得覆盖**;新状态写入独立键 `view_status`。

---

## File Structure

| 文件 | 职责 | 改动 |
|---|---|---|
| `backend/state.py` | 时间解析 + 滑窗筛选 + 状态标注(纯函数);`build_state` 的 `matches_today` | 新增 `parse_ko_dt`/`ko_status`/`decisions_view`;改 `matches_today` |
| `backend/web.py` | `/api/decisions` 路由 | 用 `decisions_view` 包一层 |
| `frontend/app.js` | 决策面板渲染 | 重写 `renderDecisions`;新增 `buildScheduleRow`/`decTopFlag`/`koTime`;`state` 加 `decFilter`/`decSel` |
| `frontend/style.css` | 赛程行/筛选条/右侧面板/响应式样式 | 追加一段 |
| `tests/test_decisions_view.py` | 新纯函数单测 | 新建 |
| `tests/test_state.py` | `matches_today` 用例 | 视情况更新 |
| `tests/test_web.py` | `/api/decisions` 用例 | 追加 |

`frontend/index.html` 不改:`renderDecisions` 全量构建 DOM 进现有挂载点 `#decision-list`。

---

## Task 1: 时间解析 + 状态判定纯函数

**Files:**
- Modify: `backend/state.py`(在 `_parse_cutoff_dt` 之后追加)
- Test: `tests/test_decisions_view.py`(新建)

**Interfaces:**
- Consumes: `backend.state.BJ`、`datetime`、`timedelta`(文件顶部已 `from datetime import datetime, timezone, timedelta`)。
- Produces:
  - `parse_ko_dt(ko_bj: str, now_bj: datetime) -> datetime | None`(把 `"M.D HH:MM"` 落到 `now_bj.year`,返回北京 aware datetime;无法解析→None)
  - `ko_status(ko_bj, now_bj, decay_h=6) -> tuple[str, datetime|None]`(状态 ∈ `"upcoming"|"recent"|"expired"|"unknown"`)

- [ ] **Step 1: 写失败测试**

新建 `tests/test_decisions_view.py`:

```python
from datetime import datetime, timezone, timedelta

from backend.state import parse_ko_dt, ko_status

BJ = timezone(timedelta(hours=8))
NOW = datetime(2026, 6, 26, 23, 0, tzinfo=BJ)  # 北京 6.26 23:00


def test_parse_ko_dt_basic():
    assert parse_ko_dt("6.27 02:00", NOW) == datetime(2026, 6, 27, 2, 0, tzinfo=BJ)


def test_parse_ko_dt_bad_returns_none():
    assert parse_ko_dt("", NOW) is None
    assert parse_ko_dt("待定", NOW) is None
    assert parse_ko_dt("6.27", NOW) is None  # 缺时间段


def test_ko_status_upcoming():
    # 同晚稍后开球 → upcoming
    assert ko_status("6.26 23:30", NOW)[0] == "upcoming"
    # 跨午夜夜场(次日日期前缀)仍是 upcoming
    assert ko_status("6.27 02:00", NOW)[0] == "upcoming"


def test_ko_status_recent_within_decay():
    # 4 小时前开球, 在 6h 窗内 → recent
    assert ko_status("6.26 19:00", NOW)[0] == "recent"


def test_ko_status_expired_beyond_decay():
    # 7 小时前开球, 超 6h 窗 → expired
    assert ko_status("6.26 16:00", NOW)[0] == "expired"


def test_ko_status_unknown():
    assert ko_status("待定", NOW) == ("unknown", None)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_decisions_view.py -v`
Expected: FAIL,`ImportError: cannot import name 'parse_ko_dt'`

- [ ] **Step 3: 实现纯函数**

在 `backend/state.py` 的 `_parse_cutoff_dt` 函数之后追加:

```python
DECAY_H = 6  # 决策卡/今日场次: 开球后保留小时数, 超过即衰减消失


def parse_ko_dt(ko_bj, now_bj):
    """ "M.D HH:MM" -> 北京 aware datetime(年份取 now_bj.year); 无法解析 -> None。

    以开球时刻为锚, 跨北京午夜的夜场(ko_bj 带次日日期前缀)天然连续。
    """
    s = str(ko_bj or "").strip()
    parts = s.split(" ")
    if len(parts) != 2 or "." not in parts[0] or ":" not in parts[1]:
        return None
    try:
        m_s, d_s = parts[0].split(".")
        hh_s, mm_s = parts[1].split(":")
        return datetime(now_bj.year, int(m_s), int(d_s), int(hh_s), int(mm_s), tzinfo=BJ)
    except (ValueError, AttributeError):
        return None


def ko_status(ko_bj, now_bj, decay_h=DECAY_H):
    """返回 (状态, dt|None)。
    upcoming: 还没开球; recent: 已开球但在 decay_h 小时内; expired: 超过; unknown: 解析不出。
    """
    dt = parse_ko_dt(ko_bj, now_bj)
    if dt is None:
        return ("unknown", None)
    if dt >= now_bj:
        return ("upcoming", dt)
    if dt >= now_bj - timedelta(hours=decay_h):
        return ("recent", dt)
    return ("expired", dt)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_decisions_view.py -v`
Expected: PASS(5 passed)

- [ ] **Step 5: 提交**

```bash
git add backend/state.py tests/test_decisions_view.py
git commit -m "feat(state): 开球时刻解析 + 滑窗状态判定(parse_ko_dt/ko_status)"
```

---

## Task 2: `decisions_view` 滑窗筛选 + 排序

**Files:**
- Modify: `backend/state.py`(接 Task 1 之后)
- Test: `tests/test_decisions_view.py`(追加)

**Interfaces:**
- Consumes: `ko_status`(Task 1)
- Produces: `decisions_view(decisions: list[dict], now_bj, decay_h=6) -> list[dict]`
  - 丢弃 `expired`;每条**浅拷贝**后加 `view_status`(`"upcoming"|"recent"|"unknown"`);按开球时刻升序,`unknown` 排末尾;非 dict 跳过。

- [ ] **Step 1: 写失败测试**

`tests/test_decisions_view.py` 追加:

```python
from backend.state import decisions_view


def _d(mk, ko):
    return {"match_key": mk, "ko_bj": ko, "status": "Selling"}


def test_decisions_view_drops_expired_keeps_rest():
    ds = [_d("A", "6.26 16:00"),  # expired (7h 前)
          _d("B", "6.26 19:00"),  # recent
          _d("C", "6.27 02:00")]  # upcoming
    out = decisions_view(ds, NOW)
    keys = [d["match_key"] for d in out]
    assert keys == ["B", "C"]            # 升序, A 被丢
    assert out[0]["view_status"] == "recent"
    assert out[1]["view_status"] == "upcoming"


def test_decisions_view_does_not_clobber_status_field():
    out = decisions_view([_d("B", "6.26 19:00")], NOW)
    assert out[0]["status"] == "Selling"  # 售卖态不被覆盖
    assert out[0]["view_status"] == "recent"


def test_decisions_view_unknown_sorts_last():
    out = decisions_view([_d("Z", "待定"), _d("C", "6.27 02:00")], NOW)
    assert [d["match_key"] for d in out] == ["C", "Z"]
    assert out[-1]["view_status"] == "unknown"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_decisions_view.py -k decisions_view -v`
Expected: FAIL,`ImportError: cannot import name 'decisions_view'`

- [ ] **Step 3: 实现**

`backend/state.py` 接 `ko_status` 之后追加:

```python
def decisions_view(decisions, now_bj, decay_h=DECAY_H):
    """筛掉 expired 的场, 每条附 view_status, 按开球时刻升序(unknown 末尾)。

    输入是 db.get_decisions() 的全量 Decision dict 列表; 返回过滤+标注后的浅拷贝列表。
    """
    out = []
    for d in decisions or []:
        if not isinstance(d, dict):
            continue
        status, dt = ko_status(d.get("ko_bj"), now_bj, decay_h)
        if status == "expired":
            continue
        out.append((dt, {**d, "view_status": status}))
    out.sort(key=lambda t: (t[0] is None, t[0] or now_bj))
    return [d for _dt, d in out]
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_decisions_view.py -v`
Expected: PASS(8 passed)

- [ ] **Step 5: 提交**

```bash
git add backend/state.py tests/test_decisions_view.py
git commit -m "feat(state): decisions_view 滑窗筛选+状态标注+按开球升序"
```

---

## Task 3: `/api/decisions` 接入 `decisions_view`

**Files:**
- Modify: `backend/web.py:36`(import)、`backend/web.py:205-211`(路由)
- Test: `tests/test_web.py`(追加)

**Interfaces:**
- Consumes: `decisions_view`(Task 2)、`db.save_decisions`、`db.get_decisions`(现有)
- Produces: `GET /api/decisions` → `{"ts": "...+08:00", "decisions": [...已过滤+标注...]}`

- [ ] **Step 1: 写失败测试**

先看 `tests/test_web.py` 顶部现有 fixture 怎么造 app/client(通常 `TestClient(create_app(...))`、`WC_DASHBOARD_NO_AUTH=1`)。仿照同款,在文件末尾追加:

```python
def test_api_decisions_filters_expired_and_tags(tmp_path, monkeypatch):
    monkeypatch.setenv("WC_DASHBOARD_NO_AUTH", "1")
    from backend.web import create_app
    from backend.db import Db
    from fastapi.testclient import TestClient
    from datetime import datetime, timezone, timedelta
    import backend.web as webmod

    db_path = tmp_path / "w.db"
    db = Db(str(db_path)); db.init()
    # 注意: ko_bj 用相对“将来/刚过去/很久前”的真实北京日期前缀, 见下方 monkeypatch now
    db.save_decisions([
        {"match_key": "A", "ko_bj": "1.01 00:00"},  # 远古 → expired, 应被滤掉
        {"match_key": "B", "ko_bj": "12.31 23:59"}, # 远未来 → upcoming, 保留
    ])
    app = create_app(db_path=str(db_path), cfg={"server": {"password": ""}})
    client = TestClient(app)
    r = client.get("/api/decisions")
    assert r.status_code == 200
    body = r.json()
    keys = [d["match_key"] for d in body["decisions"]]
    assert "A" not in keys           # 远古场被滤
    assert "B" in keys
    assert all("view_status" in d for d in body["decisions"])
```

> 说明:用"1.01"(年初)和"12.31"(年末)相对当前真实日期,确保无论何时跑测试,A 必 expired、B 必 upcoming,免依赖 freeze 时间库。

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_web.py -k decisions_filters -v`
Expected: FAIL(`view_status` 缺失 / A 仍在)

- [ ] **Step 3: 实现**

`backend/web.py` 第 36 行 import 改为:

```python
from backend.state import BJ, build_state, datetime, decisions_view
```

`backend/web.py` 的 `/api/decisions` 路由(约 205-211)改为:

```python
    @app.get("/api/decisions", dependencies=[Depends(auth_dep)])
    def api_decisions():
        now_bj = datetime.now(BJ)
        return {
            "ts": now_bj.isoformat(timespec="seconds"),
            "decisions": decisions_view(db.get_decisions(), now_bj),
        }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_web.py -k decisions_filters -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/web.py tests/test_web.py
git commit -m "feat(web): /api/decisions 经 decisions_view 滑窗过滤+标状态"
```

---

## Task 4: `matches_today` 改用同一滑窗

**Files:**
- Modify: `backend/state.py:261-273`(`build_state` 内 `matches_today` 段)
- Test: `tests/test_state.py`(追加 + 视情况更新旧用例)

**Interfaces:**
- Consumes: `ko_status`(Task 1)
- Produces: `build_state(...)["matches_today"]` 每项新增 `view_status` 字段;按开球升序;超 6h 的场不出现。

- [ ] **Step 1: 写失败测试**

先 `grep -n "matches_today" tests/test_state.py`。若已有断言"日历日"行为的用例,改其期望为滑窗行为。然后追加:

```python
def test_matches_today_window_drops_finished(tmp_path):
    from backend.db import Db
    from backend.state import build_state
    from datetime import datetime, timezone, timedelta
    BJ = timezone(timedelta(hours=8))
    db = Db(str(tmp_path / "s.db")); db.init()
    now = datetime(2026, 6, 26, 23, 0, tzinfo=BJ)
    # 远古场(应被滤) + 今晚未开球场(应在)
    db.upsert_match("100", "老队A", "老队B", "OldA", "OldB", None, "6.20 20:00", "6.20 19:00")
    db.upsert_match("101", "甲", "乙", "Jia", "Yi", None, "6.27 02:00", "6.27 01:00")
    st = build_state(db, {}, now)
    kos = [m["ko_bj"] for m in st["matches_today"]]
    assert "6.20 20:00" not in kos      # 远古被滤
    assert "6.27 02:00" in kos          # 今晚夜场保留(跨午夜不误删)
    assert all("view_status" in m for m in st["matches_today"])
```

> 注:`db.upsert_match` 签名见 `backend/db.py:103`:`upsert_match(zucai_num, home_cn, away_cn, home_en, away_en, poly_slug, ko_bj, cutoff_bj, status="Selling")`。

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_state.py -k matches_today_window -v`
Expected: FAIL(`view_status` 缺失 / 远古场仍在)

- [ ] **Step 3: 实现**

`backend/state.py` 的 `matches_today` 段(约 261-273)整段替换为:

```python
    # ---- matches_today: 开球 -DECAY_H 滑窗内的场, 按开球升序, 附 view_status ----
    mt = []
    for m in matches:
        status, dt = ko_status(m.get("ko_bj"), now_bj)
        if status == "expired":
            continue
        mt.append((dt, {
            "match": f"{m.get('home_cn', '')} vs {m.get('away_cn', '')}",
            "ko_bj": m.get("ko_bj") or "",
            "cutoff_bj": m.get("cutoff_bj") or "",
            "status": m.get("status") or "",
            "view_status": status,
        }))
    mt.sort(key=lambda t: (t[0] is None, t[0] or now_bj))
    matches_today = [x for _dt, x in mt]
```

(删除原先 `today_head`/`today_head_alt` 两行及其循环。)

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_state.py -v`
Expected: PASS(含新用例,旧用例若改过期望也通过)

- [ ] **Step 5: 提交**

```bash
git add backend/state.py tests/test_state.py
git commit -m "feat(state): matches_today 改开球滑窗(跨午夜不误删, 超6h衰减)"
```

---

## Task 5: 前端 — 筛选条 + 折叠行 + 样式(无详情面板)

**Files:**
- Modify: `frontend/app.js`(`state` 对象、新增 `decTopFlag`/`koTime`/`buildScheduleRow`、重写 `renderDecisions` 第一版)
- Modify: `frontend/style.css`(追加赛程样式段)
- Verify: Chrome headless 截图(无 pytest)

**Interfaces:**
- Consumes: 现有 `el`/`signalDot`/`flagBadge`/`topFlag`/`fmtSignedPct`(app.js 已有)、`/api/decisions` 的 `view_status` 字段(Task 3)
- Produces: `buildScheduleRow(d) -> HTMLElement`;`renderDecisions(decisions)` 渲染筛选条 + 分组折叠行进 `#decision-list`;`state.decFilter`(默认 `"upcoming"`)、`state.decSel`、`state._lastDecisions`

- [ ] **Step 1: 加样式**

`frontend/style.css` 末尾追加:

```css
/* ===== 赛程图(决策面板重做) ===== */
.dec-filter{display:flex;gap:6px;margin-bottom:12px;flex-wrap:wrap}
.df-btn{border:1px solid var(--frame);background:var(--ink-2);font:inherit;font-size:12px;
  padding:5px 12px;border-radius:var(--r);cursor:pointer;color:var(--bone-dim)}
.df-btn.on{background:var(--bone);color:var(--ink-2);border-color:var(--bone)}
.dec-group-h{font-family:var(--font-mono);font-size:12px;color:var(--bone-dim);letter-spacing:1px;
  margin:14px 0 8px;padding-bottom:4px;border-bottom:1px dashed var(--frame-soft);text-transform:uppercase}
.sched-row{display:flex;align-items:center;gap:10px;padding:10px 12px;border:1px solid var(--frame-soft);
  border-radius:var(--r);background:var(--ink-2);margin-bottom:6px;cursor:pointer}
.sched-row:hover{border-color:var(--frame)}
.sched-row.sel{border-color:var(--bone-faint);background:var(--ink-3)}
.sched-row.done{opacity:.5}
.sched-row .sr-time{font-family:var(--font-mono);font-size:13px;color:var(--bone-dim);width:50px;flex:0 0 auto}
.sched-row .sr-match{font-weight:600;font-size:14px;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.sched-row .sr-v1{font-family:var(--font-mono);font-size:12px;color:var(--bone-faint)}
.sched-row .sr-chip{margin-left:auto;display:inline-flex;align-items:center;gap:5px;font-size:11px;
  padding:2px 7px;border-radius:99px;border:1px solid var(--frame);background:var(--ink-2);flex:0 0 auto}
.sched-row .sr-chip.flag-green{border-color:var(--edge);background:var(--edge-bg)}
.sched-row .sr-chip.flag-yellow{border-color:var(--fair);background:var(--fair-bg)}
.sched-row .sr-chip.flag-red{border-color:var(--bleed);background:var(--bleed-bg)}
.sched-row .ev-pos{color:var(--edge)} .sched-row .ev-neg{color:var(--bleed)}
/* 详情面板在 Task 6 加样式 */
```

- [ ] **Step 2: 重写渲染逻辑(第一版:仅筛选条 + 行,点行只切选中)**

`frontend/app.js`:`state` 对象(约 85-91)追加三个字段:

```js
const state = {
  lastState: null,
  cutoffDeadlineMs: null,
  cdTimer: null,
  pollTimer: null,
  activeReport: null,
  decFilter: "upcoming",   // 决策面板默认筛选: 未结束
  decSel: null,            // 当前选中场 match_key
  _lastDecisions: [],      // 末次 decisions, 供筛选切换重渲染
};
```

把现有 `renderDecisions`(约 501-510)整体替换为以下函数 + 三个辅助:

```js
// 决策卡的最高级 flag (取 value.legs + best_leg)
function decTopFlag(d) {
  const legs = (d.value && Array.isArray(d.value.legs)) ? d.value.legs : [];
  const flags = legs.map((l) => l.flag).filter(Boolean);
  if (d.value && d.value.best_leg && d.value.best_leg.flag) flags.push(d.value.best_leg.flag);
  return flags.length ? topFlag(flags) : "skip";
}
// "M.D HH:MM" -> "HH:MM"
function koTime(ko) {
  const p = String(ko || "").trim().split(" ");
  return p.length === 2 ? p[1] : (ko || "—");
}
function buildScheduleRow(d) {
  const flag = decTopFlag(d);
  const row = el("div", `sched-row flag-${flag === "skip" ? "skip" : flag}`);
  if (d.view_status !== "upcoming") row.classList.add("done");
  row.appendChild(signalDot(flag));
  row.appendChild(el("div", "sr-time", koTime(d.ko_bj)));
  const home = `${d.home_flag ? d.home_flag + " " : ""}${d.home_cn || ""}`.trim();
  const away = `${d.away_flag ? d.away_flag + " " : ""}${d.away_cn || ""}`.trim();
  row.appendChild(el("div", "sr-match", home && away ? `${home} vs ${away}` : (d.match_key || "—")));
  if (d.v1 && d.v1.score != null) row.appendChild(el("div", "sr-v1", `v1 ${d.v1.score}`));
  const best = d.value && d.value.best_leg;
  if (best) {
    const b = flagBadge(best.flag);
    const chip = el("div", `sr-chip ${b.cls}`);
    chip.appendChild(signalDot(best.flag));
    chip.appendChild(el("span", null, best.desc || `${best.market || ""} ${best.outcome || ""}`.trim() || "最不亏"));
    const ev = best.ev_pct;
    if (ev != null && !Number.isNaN(Number(ev))) {
      chip.appendChild(el("span", Number(ev) >= 0 ? "ev-pos" : "ev-neg", fmtSignedPct(ev)));
    }
    row.appendChild(chip);
  }
  return row;
}

function renderDecisions(decisions) {
  const mount = $("#decision-list");
  if (!mount) return;
  decisions = decisions || [];
  state._lastDecisions = decisions;
  mount.innerHTML = "";

  // 筛选条
  const bar = el("div", "dec-filter");
  const FILTERS = [["upcoming", "未结束"], ["all", "全部"], ["edge", "仅绿"]];
  for (const [k, lab] of FILTERS) {
    const b = el("button", `df-btn${state.decFilter === k ? " on" : ""}`, lab);
    b.addEventListener("click", () => { state.decFilter = k; renderDecisions(state._lastDecisions); });
    bar.appendChild(b);
  }
  mount.appendChild(bar);

  if (decisions.length === 0) { mount.appendChild(el("div", "empty", "今日暂无决策卡, 在本地跑 /跑今天 生成")); return; }

  // 应用筛选
  let rows = decisions.slice();
  if (state.decFilter === "upcoming") rows = rows.filter((d) => d.view_status === "upcoming");
  if (state.decFilter === "edge") rows = rows.filter((d) => decTopFlag(d) === "green");
  if (rows.length === 0) { mount.appendChild(el("div", "empty", "暂无符合条件的场次")); return; }

  // 选中默认 = 第一行
  if (!state.decSel || !rows.some((d) => d.match_key === state.decSel)) state.decSel = rows[0].match_key;

  // 分组: 未结束 / 已结束(进行中)
  const groups = [
    ["今日 · 未结束", rows.filter((d) => d.view_status === "upcoming")],
    ["已结束 / 进行中", rows.filter((d) => d.view_status !== "upcoming")],
  ];
  for (const [label, arr] of groups) {
    if (!arr.length) continue;
    mount.appendChild(el("div", "dec-group-h", label));
    for (const d of arr) {
      const row = buildScheduleRow(d);
      if (d.match_key === state.decSel) row.classList.add("sel");
      row.addEventListener("click", () => { state.decSel = d.match_key; renderDecisions(state._lastDecisions); });
      mount.appendChild(row);
    }
  }
}
```

- [ ] **Step 3: 截图验证(筛选 + 行 + 默认未结束)**

用项目既有"stdlib mock server + Chrome headless"配方,造一份 mock `/api/decisions`(含 ≥1 upcoming + ≥1 recent + 各 flag),起本地服务,截图 `?#decisions`:

```bash
# 在 $CLAUDE_JOB_DIR/tmp 写一个 mock 服务(stdlib http.server, 路由 /api/* 返回 canned json, 其余落 frontend/)
# 起服务 -> Chrome headless 截图 1080 宽
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --disable-gpu \
  --force-device-scale-factor=2 --window-size=1080,1000 \
  --screenshot=$CLAUDE_JOB_DIR/tmp/t5.png "http://127.0.0.1:<port>/#decisions"
```

Expected(肉眼核对 `t5.png`):
- 顶部三按钮,**未结束**高亮(默认);
- 默认只见 upcoming 场的折叠行(`view_status==='recent'` 的不出现在"未结束"筛选下);
- 点"全部"→出现"已结束/进行中"分组、行淡化置底;
- 点"仅绿"→只剩 green 场。

- [ ] **Step 4: 提交**

```bash
git add frontend/app.js frontend/style.css
git commit -m "feat(ui): 决策面板改赛程折叠行 + 筛选条(默认未结束)"
```

---

## Task 6: 前端 — 常驻右侧面板 / 窄屏内联 + 响应式

**Files:**
- Modify: `frontend/app.js`(`renderDecisions` 接入详情面板 + `matchMedia`;`init` 加断点变化重渲染)
- Modify: `frontend/style.css`(追加 split / panel / 响应式 / 内联卡样式)
- Verify: Chrome headless 截图(桌面 + 窄屏)

**Interfaces:**
- Consumes: 现有 `buildDecisionCard(d)`(app.js:330)、`state.decSel`(Task 5)
- Produces: 桌面 `.dec-split`(左列表 + 右常驻 `.dec-panel`);窄屏选中行下内联 `.dec-inline`

- [ ] **Step 1: 加样式**

`frontend/style.css` 末尾追加:

```css
.dec-split{display:grid;grid-template-columns:1fr 372px;gap:16px;align-items:start}
.dec-list-col{min-width:0}
.dec-panel{border:1px solid var(--frame);border-radius:var(--r);background:var(--ink);
  padding:14px 16px;position:sticky;top:14px}
.dec-inline{border:1px solid var(--frame);border-top:none;border-radius:0 0 var(--r) var(--r);
  background:var(--ink);padding:12px 14px;margin:-6px 0 10px}
@media(max-width:820px){
  .dec-split{display:block}     /* 窄屏单列, 详情走 .dec-inline */
}
```

- [ ] **Step 2: 接入面板(渲染分流:桌面右栏 / 窄屏内联)**

把 Task 5 的 `renderDecisions` **分组渲染段**(`const groups = ...` 到函数尾)替换为:

```js
  const mobile = window.matchMedia("(max-width:820px)").matches;
  const sel = rows.find((d) => d.match_key === state.decSel) || rows[0];
  const groups = [
    ["今日 · 未结束", rows.filter((d) => d.view_status === "upcoming")],
    ["已结束 / 进行中", rows.filter((d) => d.view_status !== "upcoming")],
  ];

  const buildList = (container) => {
    for (const [label, arr] of groups) {
      if (!arr.length) continue;
      container.appendChild(el("div", "dec-group-h", label));
      for (const d of arr) {
        const row = buildScheduleRow(d);
        if (d.match_key === state.decSel) row.classList.add("sel");
        row.addEventListener("click", () => { state.decSel = d.match_key; renderDecisions(state._lastDecisions); });
        container.appendChild(row);
        if (mobile && d.match_key === state.decSel) {
          const card = buildDecisionCard(d);
          card.classList.add("dec-inline");
          container.appendChild(card);
        }
      }
    }
  };

  if (mobile) {
    buildList(mount);
  } else {
    const split = el("div", "dec-split");
    const left = el("div", "dec-list-col");
    buildList(left);
    const panel = el("div", "dec-panel");
    panel.appendChild(buildDecisionCard(sel));
    split.appendChild(left);
    split.appendChild(panel);
    mount.appendChild(split);
  }
}
```

- [ ] **Step 3: 跨断点自动重渲染**

`frontend/app.js` 的 `init()`(约 906)在 `setupRadarRefresh();` 之后加一行,断点变化时重渲染决策面板:

```js
  window.matchMedia("(max-width:820px)").addEventListener("change", () => {
    if ($("#view-decisions") && $("#view-decisions").classList.contains("active")) {
      renderDecisions(state._lastDecisions);
    }
  });
```

- [ ] **Step 4: 截图验证(桌面右栏 + 窄屏内联)**

复用 Task 5 的 mock 服务,截两张:

```bash
CH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
"$CH" --headless=new --disable-gpu --force-device-scale-factor=2 --window-size=1080,900 \
  --screenshot=$CLAUDE_JOB_DIR/tmp/t6-desktop.png "http://127.0.0.1:<port>/#decisions"
"$CH" --headless=new --disable-gpu --force-device-scale-factor=2 --window-size=430,1000 \
  --screenshot=$CLAUDE_JOB_DIR/tmp/t6-mobile.png "http://127.0.0.1:<port>/#decisions"
```

Expected:
- `t6-desktop.png`:左折叠行 + 右常驻面板(选中场整张决策卡,内容与旧版一致);点别的行右面板换(交互手测)。
- `t6-mobile.png`:单列,选中行**正下方**内联展开决策卡,无右栏。

- [ ] **Step 5: 提交**

```bash
git add frontend/app.js frontend/style.css
git commit -m "feat(ui): 决策面板常驻右侧详情面板 + 窄屏行内展开(复用 buildDecisionCard)"
```

---

## Task 7: 整体回归 + 收尾

**Files:** 无新增;跑全套测试 + 人工核对验收标准。

- [ ] **Step 1: 后端全套测试**

Run: `python -m pytest -q`
Expected: 全绿(含新 `test_decisions_view.py`、`test_state.py`、`test_web.py` 用例;`test_db.py::test_get_decisions_sorted_by_ko_bj_missing_last` 仍通过)。

- [ ] **Step 2: 逐条核对验收标准(对 spec §6)**

逐项确认并记录:
1. 进决策面板默认只看未结束;可切"全部 / 仅绿"。
2. 点行 → 桌面右侧常驻面板换决策卡;窄屏行内展开。
3. 赛后 >6h 的卡不再出现;已开赛未过 6h 淡化但仍在。
4. 跨北京午夜不误删当晚夜场;`matches_today` 同口径。
5. 决策卡内容与现状一致(复用 `buildDecisionCard`)。

- [ ] **Step 3: 收尾提交(如有 spec 勾选/笔记更新)**

```bash
git add -A && git commit -m "chore: 赛程图重做完成, 过验收 + 全套测试绿" || echo "无待提交变更"
```

> 报告 tab 的 mtime 排序 bug 不在本计划内,作为独立 PR 处理。

---

## Self-Review

- **Spec coverage**:§3.1 布局→Task5/6;§3.2 日期(decisions_view)→Task1/2/3;§3.2 matches_today 同口径→Task4;默认未结束→Task5;复用 buildDecisionCard→Task6;响应式 820px→Task6;§7 报告 bug 明确划出→Task7 备注。无遗漏。
- **Placeholder scan**:各步均含完整代码/命令/期望;无 TBD/TODO/"加适当错误处理"等。前端无单测,验证步用截图肉眼核对(已写明具体期望),非占位。
- **Type consistency**:`parse_ko_dt`/`ko_status`/`decisions_view`/`view_status` 全程一致;前端 `decTopFlag`/`koTime`/`buildScheduleRow`/`renderDecisions`/`state.decFilter|decSel|_lastDecisions` 命名跨 Task5/6 一致;`buildDecisionCard(d)` 复用现有签名。
