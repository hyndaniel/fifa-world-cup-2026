# 淘汰赛蒙卡 → wc-bet 工具化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 `tools/sim_match_montecarlo.py` 可被 `--input '<json>'` 编排化调用,并让 wc-bet 在判淘汰赛腿时调用它产出热力图/比分/晋级,全程挡在 Brier 之外。

**Architecture:** 工具加一个 `resolve_config()`(input JSON 覆盖 CONFIG、pen_home 默认 0.5、必填校验),`advance_prob` 参数化吃 pen_home(修掉它直读模块级 CONFIG 的 bug);`main()` 接 `--input`。机器可读输出**复用已有的 `--json <path>`**(已输出 grid+summary+advance),不另加 stdout 解析。wc-bet.md 加一条淘汰赛支路 + 诚实声明 + Brier 红线。

**Tech Stack:** Python 3 纯 stdlib(math/json/argparse),pytest 8.3.4,`from tools.sim_match_montecarlo import ...`。

## Global Constraints

- 纯 stdlib,无第三方依赖(任何 worktree/机器可跑)。
- 蒙卡输出**永不**进 Brier 跑分卡、**永不**变成 v1/v2 式预测行(同锚、非独立)。
- 不破坏现状:不带 `--input` 时,CONFIG 路径行为与现在**完全一致**。
- `pen_home` 默认 **0.5**;`p_home/p_draw/p_away/ou_line/p_over` 为必填,缺则报错退非 0。
- 改 `.claude/agents/wc-bet.md` 已获用户明确授权,用 Edit 工具(不 Bash 绕)。

---

### Task 1: 工具 `--input` 模式 + pen_home 参数化

**Files:**
- Modify: `tools/sim_match_montecarlo.py`(加 `REQUIRED`/`resolve_config`;改 `advance_prob` 签名;改 `main` 接 `--input`;docstring 加 Brier 红线 + 用法)
- Test: `tests/test_sim_match.py`(新建)

**Interfaces:**
- Produces:
  - `resolve_config(input_json: str | None = None) -> dict` — input_json 为 JSON 串(`"-"` 从 stdin 读),覆盖 CONFIG;补 `pen_home` 默认 0.5、`match/home/away` 默认占位;校验必填 + 1X2 和≈1;失败 raise `ValueError`(JSON 坏 raise `json.JSONDecodeError`)。
  - `advance_prob(l1: float, l2: float, pen_home: float) -> tuple[float, dict]` — 改成吃 pen_home 入参(原直读 `CONFIG["pen_home"]`,是 bug)。
  - `build_grid/summarize/fit` 签名不变。

- [ ] **Step 1: 写失败测试 `tests/test_sim_match.py`**

```python
import json
import pytest
from tools.sim_match_montecarlo import (
    resolve_config, advance_prob, build_grid, summarize, fit,
)

_FULL = {
    "match": "T", "home": "A", "away": "B",
    "p_home": 0.50, "p_draw": 0.27, "p_away": 0.23,
    "ou_line": 2.5, "p_over": 0.50,
}


def test_input_overrides_config():
    c = resolve_config(json.dumps(_FULL))
    assert c["home"] == "A" and c["away"] == "B"
    assert c["p_home"] == 0.50 and c["p_over"] == 0.50


def test_pen_home_defaults_half():
    c = resolve_config(json.dumps(_FULL))          # 没给 pen_home
    assert c["pen_home"] == 0.5


def test_pen_home_explicit_kept():
    c = resolve_config(json.dumps({**_FULL, "pen_home": 0.6}))
    assert c["pen_home"] == 0.6


def test_missing_required_raises():
    bad = {k: v for k, v in _FULL.items() if k != "p_over"}
    with pytest.raises(ValueError):
        resolve_config(json.dumps(bad))


def test_1x2_must_sum_to_one():
    with pytest.raises(ValueError):
        resolve_config(json.dumps({**_FULL, "p_home": 0.9}))   # 和≈1.4


def test_no_input_uses_config():
    c = resolve_config(None)                       # 走模块 CONFIG
    assert c["home"] and c["p_home"] is not None


def test_advance_prob_takes_pen_param():
    # pen 只在"加时仍平"那一小块起作用 → 越偏向主队,主队晋级条件概率越高
    hi, _ = advance_prob(1.4, 1.0, 0.9)
    lo, _ = advance_prob(1.4, 1.0, 0.1)
    assert hi > lo


def test_engine_still_fits_market():
    # 回归:拟合后 summarize 的 1X2 应贴近市场目标
    tgt = {"p_home": 0.50, "p_draw": 0.27, "p_over": 0.50}
    _, l1, l2, rho = fit(tgt, 2.5)
    s = summarize(build_grid(l1, l2, rho), 2.5)
    assert abs(s["p_home"] - 0.50) < 0.03
    assert abs(s["p_draw"] - 0.27) < 0.03
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python3 -m pytest tests/test_sim_match.py -q`
Expected: FAIL —— `ImportError: cannot import name 'resolve_config'`(以及 advance_prob 旧签名只收 2 参)。

- [ ] **Step 3: 改 `tools/sim_match_montecarlo.py` —— 加 `REQUIRED` + `resolve_config`(放在 CONFIG 常量之后、`poisson_pmf` 之前)**

```python
REQUIRED = ("p_home", "p_draw", "p_away", "ou_line", "p_over")


def resolve_config(input_json=None):
    """返回校验过的配置 dict。

    input_json 为 JSON 字符串时覆盖 CONFIG('-' 从 stdin 读);为 None 走模块 CONFIG。
    补 pen_home 默认 0.5;校验必填字段与 1X2 和≈1。失败 raise ValueError/JSONDecodeError。
    """
    if input_json is None:
        cfg = dict(CONFIG)
    else:
        if input_json == "-":
            input_json = sys.stdin.read()
        cfg = json.loads(input_json)
    cfg.setdefault("pen_home", 0.5)
    cfg.setdefault("home", "Home")
    cfg.setdefault("away", "Away")
    cfg.setdefault("match", f"{cfg['home']} vs {cfg['away']}")
    missing = [k for k in REQUIRED if cfg.get(k) is None]
    if missing:
        raise ValueError(f"缺必填字段: {', '.join(missing)}(去水概率请从 wc-odds 取)")
    s = cfg["p_home"] + cfg["p_draw"] + cfg["p_away"]
    if abs(s - 1.0) > 0.05:
        raise ValueError(f"p_home+p_draw+p_away={s:.3f} 偏离 1 太多(应去水后≈1)")
    return cfg
```

- [ ] **Step 4: 改 `advance_prob` 签名吃 pen_home(修直读 CONFIG 的 bug)**

把:
```python
def advance_prob(l1, l2):
    """90分钟平局后:加时(λ/3)→仍平则点球。返回主队晋级的条件概率。"""
    et = build_grid(l1 * ET_SCALE, l2 * ET_SCALE, 0.0)
    et_home = et_draw = et_away = 0.0
    for (i, j), p in et.items():
        if i > j:
            et_home += p
        elif i == j:
            et_draw += p
        else:
            et_away += p
    pen = CONFIG["pen_home"]
```
改成:
```python
def advance_prob(l1, l2, pen_home):
    """90分钟平局后:加时(λ/3)→仍平则点球。返回主队晋级的条件概率。"""
    et = build_grid(l1 * ET_SCALE, l2 * ET_SCALE, 0.0)
    et_home = et_draw = et_away = 0.0
    for (i, j), p in et.items():
        if i > j:
            et_home += p
        elif i == j:
            et_draw += p
        else:
            et_away += p
    pen = pen_home
```

- [ ] **Step 5: 改 `main()` —— 加 `--input`、走 `resolve_config`、传 pen 给 advance_prob**

把 `main()` 里参数解析 + 取 config 段:
```python
    ap.add_argument("--json", metavar="PATH",
                    help="另存比分网格 JSON(字符串键 'i-j': prob)")
    args = ap.parse_args()

    c = CONFIG
    line = c["ou_line"]
    if c["p_home"] is None:
        print("✗ CONFIG 里的去水概率还没填(p_home/p_draw/p_away/p_over)。先跑 wc-odds 取数。",
              file=sys.stderr)
        return 2
```
改成:
```python
    ap.add_argument("--json", metavar="PATH",
                    help="另存比分网格 JSON(字符串键 'i-j': prob)")
    ap.add_argument("--input", metavar="JSON",
                    help="JSON 配置覆盖 CONFIG('-' 从 stdin 读);供 wc-bet 编排化调用")
    args = ap.parse_args()

    try:
        c = resolve_config(args.input)
    except (ValueError, json.JSONDecodeError) as e:
        print(f"✗ 输入无效: {e}", file=sys.stderr)
        return 2
    line = c["ou_line"]
```
并把后面这行:
```python
    p_adv_given_draw, et = advance_prob(l1, l2)
```
改成:
```python
    p_adv_given_draw, et = advance_prob(l1, l2, c["pen_home"])
```

- [ ] **Step 6: 改 docstring —— 加 Brier 红线 + `--input` 用法**

在模块顶部 docstring 的「口径(诚实声明)」块末尾追加一句:
```
  ⚠️ Brier 红线: 本工具市场锚定、与 v2 同锚,非独立信号 —— 输出只供 wc-bet 决策内部用
     (比分网格/热力图/晋级%), 永不当 v1/v2 式预测、永不进 Brier 跑分卡。
```
并在「用法」块补一行:
```
  编排化(wc-bet 用): python3 tools/sim_match_montecarlo.py --input '<json>' --json <path>
    input JSON: {match,home,away,p_home,p_draw,p_away,ou_line,p_over,pen_home?}; pen_home 默认 0.5
```

- [ ] **Step 7: 跑测试确认全过**

Run: `python3 -m pytest tests/test_sim_match.py -q`
Expected: PASS(8 passed)。

- [ ] **Step 8: 端到端冒烟 —— 确认 `--input` 报告 + JSON 文件都对**

Run:
```bash
python3 tools/sim_match_montecarlo.py --input '{"match":"R32-76 BRA vs JPN","home":"Brazil","away":"Japan","p_home":0.555,"p_draw":0.258,"p_away":0.187,"ou_line":2.5,"p_over":0.46}' --json "$CLAUDE_JOB_DIR/tmp/smoke_grid.json" | tail -8
python3 -c "import json;d=json.load(open('$CLAUDE_JOB_DIR/tmp/smoke_grid.json'));print('keys',sorted(d));print('adv',d['advance']);assert abs(sum(d['grid'].values())-1)<1e-6"
```
Expected: stdout 末尾打印晋级行 + "网格已存";第二条打印 `keys ['advance','dc_rho','grid','lambda_away','lambda_home','match','summary']`、`adv {...home/away...}`、网格归一化通过(无 AssertionError)。

- [ ] **Step 9: 提交**

```bash
git add tools/sim_match_montecarlo.py tests/test_sim_match.py
git commit -m "feat(sim): --input JSON 模式 + pen_home 参数化(默认0.5),供 wc-bet 编排化调用

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: wc-bet 接入淘汰赛蒙卡支路

**Files:**
- Modify: `.claude/agents/wc-bet.md`(§4 工作流加淘汰赛支路;§5 加蒙卡诚实声明;§7 红线加蒙卡条)

**Interfaces:**
- Consumes: Task 1 的 `--input '<json>' --json <path>` 工具契约。
- Produces:(无代码接口;agent 行为说明)

- [ ] **Step 1: §4 工作流末尾加淘汰赛支路段**

在 `.claude/agents/wc-bet.md` §4 的 `**工作流**` 那段之后,新增一段:
```markdown
**淘汰赛支路(仅淘汰赛/有晋级诉求时)**:用 `tools/sim_match_montecarlo.py` 把 wc-odds 的去水 1X2 + 大小球线落成比分网格:
`python3 tools/sim_match_montecarlo.py --input '{"match":..,"home":..,"away":..,"p_home":..,"p_draw":..,"p_away":..,"ou_line":..,"p_over":..,"pen_home":0.5}' --json <tmp.json>`(`pen_home` 默认 0.5=点球近抛硬币、杠杆极低,一般不动)。读 stdout 的**热力图+Top比分+晋级%** 摆进决策卡/复盘台账;读 `<tmp.json>` 的 `grid` 算**让球腿/净胜N球/大小球腿**真实命中(同向腿不连乘、深盘高门槛=陷阱、全包=零边际价值);`advance` 的晋级% 跟 **Polymarket 晋级盘**交叉验证。
```

- [ ] **Step 2: §5 结算/陷阱段末加蒙卡诚实声明**

在 §5 `**结算/陷阱(讲清)**` 段末尾追加:
```markdown
 **蒙卡诚实口径**:`sim_match_montecarlo` 的 1X2 与市场**同锚**、不是独立证人(别拿它"印证"市场);比分形状是模型假设(二元泊松独立 + Dixon-Coles ρ),非实测——铁桶与对攻同 1X2 会得同网格。它的价值是把市场 1X2 摊成比分/晋级分布供算腿,不是新信号。
```

- [ ] **Step 3: §7 红线加蒙卡条**

在 §7 末尾追加一条:
```markdown
- **蒙卡不进跑分卡**:`sim_match_montecarlo` 输出(网格/热力图/晋级)只活在你决策内部,与 v2 同锚、非独立 → 永不当 v1/v2 式预测行落库、永不进 Brier。
```

- [ ] **Step 4: 一致性自查**

Run: `python3 - <<'PY'`(或人工)确认三处文本都已落、§7 现在有蒙卡条:
```bash
grep -n "蒙卡" .claude/agents/wc-bet.md
```
Expected: 至少 3 处命中(§4 支路、§5 声明、§7 红线)。

- [ ] **Step 5: 提交**

```bash
git add .claude/agents/wc-bet.md
git commit -m "feat(wc-bet): 淘汰赛蒙卡支路 — 调 sim 出热力图/晋级算腿, 同锚非独立挡在 Brier 外

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec coverage:**
- spec A(工具 --input/默认 pen_home/必填校验/双输出)→ Task 1 全覆盖。注:机器可读输出**复用已有 `--json <path>`** 而非新增 `--emit-json`(YAGNI:`--json` 已输出 grid+summary+advance,wc-bet Read 文件即可,免 stdout 混合解析)。这是对 spec 的实现简化,功能等价。
- spec B(wc-bet 调用/展示/算腿/晋级交叉)→ Task 2 全覆盖。
- spec C(Brier 红线 docstring + wc-bet.md)→ Task 1 Step 6 + Task 2 Step 3。
- spec 测试①②③ → Task 1 Step 7-8(单测+冒烟)覆盖①;Task 2 为 agent 行为,端到端②由用户实跑一场验证(plan 外,落地后做)。

**2. Placeholder scan:** 无 TBD/TODO;每个改代码的 step 都给了完整 old→new 文本。

**3. Type consistency:** `resolve_config` / `advance_prob(l1,l2,pen_home)` 在 Task 1 定义并在 main 调用,签名一致;wc-bet 调用的 `--input/--json` 与 Task 1 的 argparse 一致。
