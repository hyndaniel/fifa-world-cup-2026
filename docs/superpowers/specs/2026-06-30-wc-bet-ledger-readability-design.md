# wc-bet 下注复盘 可读性改版 · 设计

**日期**：2026-06-30　**对象**：`reports/agents/wc-bet__下注复盘.md`（661 行，6.24→6.30）
**目标**：整份替换为「结论先行 + 可扫读」格式。读者＝用户本人（懂术语）。**保留全部数字与真分析**，只重排呈现、砍散文墙。
**范围调整（用户 2026-06-30）**：**删除「我和朋友的实购票复盘」两节**——属过程产物、后续不维护；只保留并重排 wc-bet 自己的**赛前推荐腿台账**（TYPE A）。故 TYPE B 模板取消。

## 设计原则（4 条）

1. **每节标题自带状态**：`## 日期 · 场次 · 🚦档 · 下/不下`（或票节：`· 全-EV 🎲`）。
2. **结论一句话置顶**：每节/每场开头一条 `> 一句话` 大白话结论 + 一条 `> 赛后` 实际兑现。
3. **赛前用表不用散文**：价值/票面信息进紧凑表，删掉重复的连续段落。
4. **赛后 ✅❌ 并进同一张表**（不另起「实际」长段）；深层逐腿明细用 `<details>` 折叠，scannable 在外、细节一键展开。

## 两套模板

### TYPE A — 赛前价值台账（6.24 价值表节 / 6.26 / 6.27 / 6.28 / 6.29 / 6.30）

每场一个块：

```
### 074 巴西 vs 日本 · R32 · 🔴 空仓
> 一句话：巴西被压成全场最贵热门(1.49)，买它=贵、赢了也赚少；其余腿全偏亏 → 不下。
> 赛后：实际 2-1 ｜ 最不亏腿没中 ❌(净胜1=让平档) ｜ v1 的 2-1 让球味命中 ✅

价值速览　value=竞彩赔×Poly真概率；🟢≥1.03值 / 🟡0.97–1.03守 / 🔴<0.97亏
| 腿 | 赔率 | value | 档 | 赛后 |
|---|---|---|---|---|
| 巴西胜 | 1.49 | 0.85 | 🔴 压最短 | ✅ |
| …（含 best_leg 加粗+〔最不亏〕标）| | | | |

🎲 skip/陷阱：…（一行）
```

- 多场的节（如 6.29 三场）= 三个这样的块 + 节末一个「当天复盘」块。
- value 表头里的口径说明（had/hhad/网格/软锚警示）压成表下一行小字，不再整段。

### TYPE B — 实购票横向复盘　❌ 取消（删除，不重排）

用户定为过程产物、后续不维护。**整节删除**：
- `## 6.24 周三 049–054 · 五张实下票横向复盘（清醒彩票口径）`
- `## 6.26 20:56 三脑重跑 · 用户实购 6 场 3 串 1 复盘（清醒彩票口径）`

（注：`data/bet_ledger.json` 的 `tickets[]` 与 `/api/bets/summary` 实购面板是另一处独立 surface，本次**不动**，待用户另定是否一并退役。）

## 全局结构

1. **顶部** `# 盘口价值 · 推荐腿复盘` + 两行 disclaimer（保留）。
2. **名词&黑话**：保留全部条目（自读参照），但折进 `<details>`，不占开头视线。
3. **正文**：删除上述两节实购复盘后，**剩余全部为 TYPE A 推荐腿台账**，按时间序逐节重排。其余日期节一个不删。
4. **每节末尾的「数据镜像/非投注建议」尾巴**：压成一行小字。

## 红线（验收门槛）

- ✅ 两节实购票复盘已删；**其余所有日期节都在、顺序不变**（6.24×2 / 6.26 055-060 / 6.27 / 6.28 / 6.29 / 6.30×3）。
- ✅ 每条腿的 **赛后 ✅❌ / 实际盈亏 / 命中率** 一个不丢（赛后结果是台账核心资产）。
- ✅ 所有 value / EV% / 赔率 / Poly去水 数字保留（自读要自核）。
- ✅ best_leg、分档、软锚警示、整数让球走盘陷阱等**判断性结论**保留（这是报告的智力价值）。
- ✅ 与 `data/bet_ledger.json`、`复盘对错标` 不冲突（只改叙事、不改数据）。
- ❌ 不新增/不篡改任何赛果或数字；纯重排。

## 执行

- 在 worktree 分支 `reformat-bet-ledger-readable` 上，派 wc-bet 子代理(opus)按本 spec 整份重写（单代理单文件、保一致），主会话逐节验收红线（节数/赛后标/数字抽查），过后 PR + 合并(需授权) + ff 本地 main。
