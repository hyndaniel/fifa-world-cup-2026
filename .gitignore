# Python
__pycache__/
*.py[cod]
.venv/
venv/

# 数据 / 密钥 (不入库)
*.db
*.sqlite
*.sqlite3
.env
config.toml

# 前端
node_modules/

# OS
.DS_Store

# 运行时态 / agent 态 (不入库)
.cache/
.claude/agent-memory/
docs/superpowers/handoffs/

# 报告 PDF 导出 (可从 reports/*.md 100%重新渲染, 不入库避免仓库体积膨胀)
reports/pdf/
reports/match-sims/pdf/

# SQLite 运行时文件 (WAL/共享内存, 随 wc.db 自动生成, 不入库)
*.db-shm
*.db-wal
*.db-journal
